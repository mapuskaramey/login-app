import validate from './validate.js'

export default {
    methods: {
        validateForm () {
            console.log(this.user)
            alert('Thank you for registration with us!')
        }
    }
}